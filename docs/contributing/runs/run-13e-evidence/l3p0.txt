xhcisnap: opening \\.\HCD0

route probe - IOCTL 0x00220438 on this controller:
  PassThru, our GUID                 status  6 (MINIPORT DECLINED)
    the request reached a miniport and it DECLINED. The ROUTE WORKS.
    Two things look exactly like this and the driver cannot tell you which:
      1. an xhci98 whose channel is switched off - the default on every
         machine. Fix: xhcisnap -verbosity 2, then RESTART, reproduce, and
         dump.
      2. some other miniport on this HCD index, or an xhci98 from before the
         channel shipped. Try -c 1, -c 2, or check the installed xhci98.sys.
  unknown request code 15            status  2 (invalid request code)
    as derived: the dispatcher's request table has 8 entries and 15 is not one
      of them
  RequestBufferLength disagreeing    status  4 (invalid header parameter)
    as derived: the header's own length must equal the IOCTL's input length
      exactly
  a 0x20-byte buffer                 status  7 (buffer too small)
    as derived: PassThru's floor is 0x28 - a 0x10 header plus a 0x18 body
